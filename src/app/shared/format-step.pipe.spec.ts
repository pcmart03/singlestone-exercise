import { FormatStepPipe } from './format-step.pipe';

describe('FormatStepPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStepPipe();
    expect(pipe).toBeTruthy();
  });
});
